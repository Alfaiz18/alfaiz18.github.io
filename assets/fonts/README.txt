Fonts (Syne, DM Sans, Space Mono) are currently loaded from Google Fonts
via <link> tags in the HTML <head>. If you'd rather self-host for privacy
or offline reliability, download the .woff2 files into this folder and
update the @font-face rules / <link> tags accordingly.
